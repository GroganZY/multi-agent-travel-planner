# 基础（milvus_lite 依赖 pkg_resources，需 setuptools<82）
setuptools>=69.0.0,<82

# 多智能体框架
agentscope==1.0.16

# LLM 调用
requests==2.32.3

# RAG 与向量库（pymilvus[milvus_lite] 用于本地 .db 连接）
pymilvus[milvus_lite]==2.6.9
sentence-transformers==5.2.3

# CLI 界面
rich==13.9.4

# 网络搜索（DDGS）
ddgs==9.10.0

# PostgreSQL 异步驱动
asyncpg==0.30.0

# Redis 异步客户端
redis[hiredis]==5.2.1

# RAG 评估框架（可选，使用 run_eval_ragas.py 时需安装）
# ragas>=0.2.0
# datasets>=2.0.0
